# GitHub Pages setup for GitHub-Uploader-

**Required:** Settings → Pages → Build and deployment → Source → **GitHub Actions**.

The live URL is:

https://matthewcodergamer.github.io/GitHub-Uploader-/

The repository root contains Vite/React source. GitHub Pages must not publish the source branch directly. The included workflow compiles the app first and deploys `dist/`.
